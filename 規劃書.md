# 小朋友下樓梯（stair-kids）規劃書

> 遊戲正式名稱：**小朋友下樓梯**，專案代號 **`stair-kids`**，資料夾 `C:\Eric\AI Agent\stair-kids`。
>
> 狀態：**規劃完成，等你點頭就開工**。本文件是決策紀錄與施工圖。
>
> 沿用 `C:\Eric\AI Agent` 既有小遊戲的架構慣例：Node 後端、`public/` 靜態前端、
> `public/js/rules.js` 共用純函式規則核心、`lib/rooms.js` 房間與權限、
> `public/js/config.js` 唯一 server URL 入口、`tests/verify.js`、`scripts/*-check.js`、
> `render.yaml`、`啟動遊戲.bat`，介面／註解／文件／commit 全繁體中文。
> 連線做法比照最近的 **bubble-battle（泡泡大作戰）**：**零依賴**，自寫 WebSocket 伺服器（`lib/ws.js`），
> 不引入 Express／Socket.IO。

---

## 0. 已確認的決定

以下每一項都是你在規劃問答中親自選的，實作期間不再自行變動；要改請直接說。

### 0.1 玩法核心

| 項目 | 決定 |
|---|---|
| 對戰形式 | **同畫面共用一座樓梯**（經典雙人），不做分割畫面 |
| 對戰人數 | **固定 2 人** |
| 生命 | **血量制**：踩到刺階扣血、被上方天花板刺頂住持續扣血 |
| 血量顯示 | **愛心排**；幼幼班 20 顆改用 **愛心＋數字**（`❤ ×20`） |
| 階梯種類 | **經典五種**：普通、輸送帶、彈簧跳床、刺階、假階 |
| 刺階傷害 | **扣 1 顆愛心 ＋ 0.6 秒無敵閃爍**，不做僵直 |
| 彈簧強度 | **溫和：彈回上一層半**，只做一種彈簧 |
| 輸送帶 | **能逆向走，但變很慢**（6 − 3 ＝ 3 格／秒） |
| 道具 | **完全不做**，純比操作技巧 |
| 操作 | **只有左右**（純經典），沒有跳、沒有下壓 |
| 鏡頭 | **跟「下得最深的存活者」**，落後者被頂到天花板持續扣血 |
| 保底下捲 | **要做，而且隨時間越來越快**（原版「越玩越快」的節奏，避免兩人一起賴在最上面） |
| 玩家碰撞 | **會互相推擠**（可以把人推去撞刺、推離安全落點），**不能互踩頭** |
| 分數 | **深度（公尺）就是唯一分數**，不加其他加分項 |
| 里程碑 | **每 100 公尺慶祝提示 ＋ 換下一層世界**，**不做橫貫全寬的大平台** |
| 開局出生 | **同一個大平台上左右分開站，倒數 3 秒** |
| 幼幼班天花板 | **改畫成軟綿綿的雲朵，完全不畫尖刺**（頂到只被輕輕推回） |
| 換世界過場 | **0.8 秒漸變，不打斷操作**（樓梯不停、不擋畫面） |

### 0.2 模式與勝負

| 項目 | 決定 |
|---|---|
| 單機模式 | **一人無限挑戰** ＋ **跟 1 個電腦對戰**（同一套共用樓梯規則） |
| 難度 | **四段：幼幼班／簡單／普通／困難**，行為差異必須看得出來 |
| 幼幼班 | **大幅放寬、鼓勵式不死**：速度很慢、不出刺階與假階、被頂只推回不扣血、血量 20 |
| 幼幼班結束 | **沒有終點**，隨時按「結束這局」看成績 |
| 幼幼班對戰 | **不開放**（不會死就分不出勝負），對戰只有簡單／普通／困難 |
| 線上勝負 | **兩個人都死了才結算**，先死的人不會立刻結束對局；比最終深度 |
| 單機對電腦勝負 | **玩家一死就結束**，不必等電腦死；以雙方當下深度比較 |
| 斷線 | **一斷線就判輸**，不暫停、不給寬限秒數，對手照常玩完 |

### 0.3 線上與介面

| 項目 | 決定 |
|---|---|
| 觀戰 | 2 席滿了自動轉觀戰（**上限 20 人**），**可聊天、可看全場** |
| 席位遞補 | **不做候補排隊**，有人離開就**開放搶位**（先按「坐下」先得） |
| 暱稱 | **對戰可自訂**；不自訂就給**隨機可愛名**（「形容詞＋小動物」自動組合），記住上次用的暱稱與角色 |
| 聊天 | **自由輸入 ＋ 快捷短語**（不會打字的小朋友也能溝通） |
| 觸控 | **左下角左右兩顆大按鍵**（不做全螢幕左右半邊，也不做切換） |
| 直向按鍵 | **手機／平板直向改成左鍵左下、右鍵右下**（兩手拇指各一邊）；橫向維持兩顆都在左下 |
| 角色辨識 | 自己腳下光環＋頭頂暱稱，**再加玩家色外框（玩家 1 藍、玩家 2 紅）** |
| 深度顯示 | **只顯示兩人各自的深度數字**，不顯示領先／落後差距、不跳落後警告 |
| 音樂 | **木琴童謠風**，同一段主旋律**每個世界換樂器變奏** |
| 結算 | 勝利者大頭貼＋一行數據，**再加一小區「這局統計」** |
| 單機快捷鍵 | **只有 `Esc` 暫停選單**，不做 R 快速重來（避免小朋友誤觸） |
| 大廳 badge | 標 **「新遊戲」** |
| 觀戰介面 | **跟玩家看到的完全一樣**，資訊欄再加一行「你是觀戰者」 |
| 暫停選單 | **只有三顆：繼續／重新開始／回首頁**（設定走右上角，不做重複入口） |
| 再來一局 | **每局都換新樓梯（新 seed）**，不做同座樓梯重打 |
| 幽靈重播 | **不做**（單機保持單純，不放半透明的自己） |
| 遊戲手把 | **不支援**，只做鍵盤與觸控 |
| 房間回收 | **沒人就立即關房**（免費方案不留空房），關房時邀請連結一起失效 |
| 資訊欄 | 依你的偏好放**左邊、刻意小**，樓梯畫面盡量大；聊天室固定左下 |
| 教學 | **靜態圖文說明頁**，不做互動練習關 |
| 角色 | **小朋友人物 8 隻**，手繪向量 SVG（頭＋身體＋手＋腳、漸層立體），能力完全相同 |
| 樓梯主題 | **六套，隨深度每 100 公尺換下一層世界**（幼稚園→公園→海邊→森林→糖果屋→太空站） |
| 紀錄 | **只存本機**：各難度最深記錄＋對戰勝負，不做伺服器排行榜 |
| 版控 | **現在就建本機 git**，公開 repo 等 **M0 可玩**再建（名稱 `stair-kids`） |
| 本機 port | **3060**（3000／3010／3020／3030／3040／3050／3090 都被佔了） |
| 語言 | **只做繁體中文**，不抽語言檔 |

---

## 1. 玩法規則

### 1.1 場地與座標

- 邏輯座標以「格（unit）」為單位，與畫素解耦，所有規則計算都在邏輯座標做。
- 場地寬 **12 格**，左右是牆（撞牆停住，不繞回）。
- 可見高度 **18 格**；`depth` 為往下的正值，**深度就是分數**（1 格 = 1 公尺）。
- 畫面最上緣是**天花板刺**，永遠貼著鏡頭上緣移動，是唯一的「追殺者」。

### 1.2 角色移動與物理

| 參數 | 值 | 說明 |
|---|---|---|
| 角色尺寸 | 寬 1.0 格、高 1.6 格 | 碰撞用 AABB |
| 水平速度 | 6.0 格／秒 | 按住就是等速，無加速度（好預測，小朋友好上手） |
| 重力 | 30 格／秒² | |
| 最大落速 | 18 格／秒 | 避免穿階 |
| 落地判定 | 只在**下墜中**才會踩到階梯 | 上升時（彈簧）穿過階梯，經典手感 |
| 穿階防護 | 每步做**掃掠檢查（swept AABB）** | 高速落下不會漏踩 |

- 沒有跳、沒有下壓；想往上只能靠彈簧階。
- 站在階梯上會被階梯的水平效果帶動（輸送帶）。
- 落到畫面下緣不會死（會被鏡頭追上而回到畫面內），**唯一的死法是血歸零**。

### 1.3 五種階梯

| 階梯 | 外觀 | 效果 |
|---|---|---|
| **普通階** | 木頭／磚塊 | 站著不動 |
| **輸送帶** | 滾輪＋明顯方向箭頭，滾動動畫 | 站上去被帶動 ±3.0 格／秒；**可逆向走但只剩 3.0 格／秒** |
| **彈簧跳床** | 螺旋彈簧＋壓縮動畫 | 踩到向上彈初速 9 格／秒（**約回到上一層半**，只有這一種強度） |
| **刺階** | 紅底＋三角尖刺 | 踩到 **−1 愛心**，並給 **0.6 秒無敵閃爍**避免連續扣；不僵直 |
| **假階** | 顏色偏白、邊緣裂痕 | 踩到 0.25 秒後崩解消失，只能當短暫落腳點 |

- 依你「要能清楚辨別避免玩家誤判」的標準：**五種都用不同形狀＋不同顏色＋不同動態**辨識，
  不只靠顏色（色彩輔助預設就靠形狀區分）。
- 輸送帶方向靠**箭頭與滾輪轉動**表達，不靠顏色深淺。

### 1.4 鏡頭與天花板（核心壓力來源）

- 鏡頭 `cameraTop` 每 tick 取兩者較大值：
  1. **最深存活玩家的深度 − 12 格**（領先者維持在畫面下方約 1/3）
  2. **上一格鏡頭位置 ＋ 保底下捲速度 × dt**
- **保底下捲速度隨時間加快**（速度表見 1.7）。這是你確認的規則：等於原版「越玩越快、遲早會死」的節奏，
  局長自然收在 2～4 分鐘，也保證兩個人不能一起賴在最上面。
- 鏡頭**只跟存活者**。線上對戰先死的人不影響鏡頭；剩下的人繼續往下跑自己的深度。
- 天花板刺永遠在 `cameraTop`。**幼幼班例外：改畫成軟綿綿的雲朵、完全不畫尖刺**（見 1.7）。
- 角色被壓在天花板上時：
  - 持續扣血（間隔依難度，見 1.7），扣血瞬間有紅色閃爍＋畫面輕微震動＋警告音。
  - 只被「推回」畫面內，不會被擠出畫面外。
- 鏡頭永遠不回捲（彈簧彈再高，鏡頭也只停住不上升）。

### 1.5 血量與傷害

- 血量預設 **10**（難度會調），介面用**愛心排**顯示，扣血時該顆愛心破掉。
  - 幼幼班 20 顆愛心排太長，改成 **`❤ ×20`（愛心＋數字）**。
- 傷害只有兩種來源：**踩刺階（−1）**、**被天花板頂住（依難度間隔 −1）**。
  撞牆、掉出畫面下緣、被推擠都不扣血。
- 扣血後 **0.6 秒無敵閃爍**，期間不再受同類傷害。
- 血歸零 → 該玩家**淘汰**：角色定格成「暈眩」動畫，深度數字鎖定。

### 1.6 分數與勝負

- **分數 = 最深深度（公尺）**，只增不減，全程顯示在資訊欄。
- **每 100 公尺**跳一次慶祝提示（`100m！`＋音效＋紙花），同時**換下一層世界**（見 8 節）。
  **不做橫貫全寬的休息平台**，天花板照樣追上來。
- **一人挑戰（單機）**
  - 簡單／普通／困難：血歸零就結束，結算顯示這次深度與該難度歷史最深。
  - **幼幼班**：不會死、沒有終點，畫面右上角提供 **「結束這局」** 按鈕（Esc／返回鍵也會叫出暫停選單），
    按了就看成績，想玩多久玩多久（這顆鈕放在右上角設定鈕旁邊，不進 Esc 暫停選單）。
- **線上 1v1（你指定的規則）**
  - **一個人死不會結束對局。** 先死的人切成「對手視角」繼續看、可以聊天，自己的深度已鎖定並顯示。
  - **兩個人都死了才結算。** 勝負依 **最終深度較深者勝**；深度相同比**存活時間**較長者勝；兩者都同判**平手**。
  - 因為鏡頭跟存活者，後死的人通常能再往下跑一段；但如果他撐不久、死得比對手淺，
    **先死的人照樣會贏**，所以「趕快下去」和「小心不要死」都有意義。
  - **斷線者直接判輸**（見 4.4），對手可以繼續玩完把深度跑滿。
- **單機對電腦（你指定的規則）**
  - **玩家一死就結束**，不必等電腦死。
  - 判定用**雙方當下深度**比較（電腦若已先死，用它死亡時鎖定的深度）；同深度比存活時間。
- 一局定勝負，不做三戰兩勝、不做局間累計。結算後回房間由房主開下一局。

### 1.7 難度四段

| 難度 | 保底下捲速度 | 加速 | 速度上限 | 血量 | 刺階比例 | 假階比例 | 天花板扣血 |
|---|---|---|---|---|---|---|---|
| **幼幼班** | 1.2 格／秒 | 每 30 秒 +5% | 1.5× | 20（`❤ ×20`） | **0%（不出現）** | **0%（不出現）** | **不扣血，只推回** |
| **簡單** | 2.0 | 每 20 秒 +10% | 2.0× | 12 | 8% | 8% | 每 0.9 秒 −1 |
| **普通** | 2.8 | 每 20 秒 +12% | 2.2× | 10 | 14% | 14% | 每 0.6 秒 −1 |
| **困難** | 3.6 | 每 20 秒 +14% | 2.5× | 8 | 20% | 18% | 每 0.4 秒 −1 |

- 幼幼班照你的決定**大幅放寬到「鼓勵式不死」**，並且**不開放對戰**（見 0.2）。
- 幼幼班的天花板**改畫成軟綿綿的雲朵，完全不畫尖刺**：頂到只會被輕輕推回來，配「啵」的軟音效，
  小朋友不會因為看到一排尖刺而緊張；其他三段難度照樣是刺。
- 對戰（含跟電腦）由**房主選一個共用難度**（預設普通），兩人條件完全相同。

---

## 2. 玩家推擠（對戰唯一的互動）

沒有道具，所以「推擠」是對戰全部的互動來源，手感要調到夠有干擾感但不失控：

- 兩個角色的 AABB 重疊時，**沿水平方向把雙方各推開一半**（對稱、不看誰先動），
  垂直方向不推（所以不會互踩、不會疊人）。
- **推不動的情況**：對方另一側貼牆時，推的人自己被擋住（可以卡牆但不會穿模）。
- 推擠**不扣血**，但可以把人推到刺階上、推離安全落點、推去撞天花板刺 —— 傷害由環境造成。
- **把人推上輸送帶**是很有效的招（他要花時間逆走出來），這是這款對戰的主要戰術。
- 兩人都在天花板下被夾住時，**兩個人一起扣血**（不會有人被當肉墊擋刺）。
- 不能互踩頭：從上方落到對方身上時，落下者被水平推開到旁邊，不會停在對方頭上。
- 同步公平性：推擠完全由**伺服器權威**計算（見第 4 節），不看誰的網路快。

---

## 3. 樓梯程序生成（必須可重現）

- 用**可注入種子亂數** `rng.js`（沿用既有小遊戲的做法），`seed` 由伺服器發、單機自己產。
  同一個 seed 一定長出同一座樓梯，讓 AI 測試與線上同步都能重現。
  **每一局都發新的 seed**（「再來一局」也是新樓梯），不做同座樓梯重打、不做幽靈重播、不做 seed 分享。
- 樓梯是**無限往下程序生成**，一次只保留鏡頭附近的區段（上下各約 30 格），舊的丟掉。
- 生成規則（每一層一階，偶爾兩階）：
  - 層間垂直間距 **2.0～3.0 格**隨機。
  - 階梯寬 **2.5～4.0 格**隨機。
  - **可達性保證**：新階與上一層的水平距離不得超過「從上一層落到這一層的時間 × 6 格／秒 × 0.8」，
    保證用走路速度一定來得及走到；不合格就重抽。
  - **連續同款上限**：同一種特殊階梯不連續超過 2 層；刺階不連續出現。
  - **保底安全落點**：每 4 層至少有一層是普通階或輸送帶（不會出現連續刺／假階的死路）。
  - 每 12 層放一個**較寬的普通階**（寬 5 格）當喘息點 —— 只是比較寬的一階，
    **不是橫貫全寬的平台**，天花板照樣會追上來。
  - 彈簧階不緊接在刺階正下方（避免彈上去馬上被頂又落回刺上）。
  - 開局第 0 層是**寬 8 格的出生大平台**（見 7.3），只有這一層是例外。
- 生成器是**純函式**：`makeStairs(seed, difficulty, fromDepth, toDepth) → 階梯陣列`，
  伺服器與瀏覽器 require 同一支，測試可以直接跑一萬層檢查可達性與比例。

---

## 4. 連線同步（最關鍵的技術決定）

共用同一座樓梯又要互相推擠，**不能各自算各自的**，所以：

### 4.1 伺服器權威 + 固定 tick

- 伺服器以 **30 Hz（33.3 ms）** 跑 `stepMatch()`，是唯一真相來源。
- 客戶端只送**輸入意圖**：`{ seq, dir: -1 | 0 | 1, dtMs }`，不送座標。
- 伺服器每 tick 廣播快照：`{ tick, cameraTop, players[], newStairs[], events[] }`
  - 玩家：位置、垂直速度、血量、深度、狀態（正常／無敵／被頂／淘汰）。
  - 樓梯用**增量**傳（只傳新生成與消失的階梯），不每 tick 傳整座樓梯。
  - 事件：踩刺、彈簧、假階崩解、被頂扣血、100 公尺里程碑、淘汰 —— 給前端播音效與特效。

### 4.2 前端手感

- **本地預測**：自己的角色照本地輸入立刻動（左右移動與重力都在前端跑同一支 `rules.js`）。
- **對帳**：收到快照後把自己校正到伺服器位置，再重播尚未確認的輸入。
- **對手插值**：保留約 100 ms 緩衝，在兩份快照之間平滑內插，不外插（避免瞬移回彈）。
- 推擠是最容易「打架」的地方：**推擠結果一律以伺服器為準**，前端預測到的推擠若被否決，
  用 80 ms 的平滑修正拉回去，不硬切位置。

### 4.3 單機與線上共用同一套規則

`public/js/rules.js` 是**純函式規則核心**（無 DOM、無 socket、無計時器）：

- `createMatch(config, seed) → state`
- `stepMatch(state, inputsByPlayer, dtMs) → { state, events[] }`
- `makeStairs / resolveLanding / resolvePush / applyCeiling / checkResult`

單機在瀏覽器自己跑 `stepMatch`，AI 產生與真人**格式完全相同**的輸入意圖；
線上由 Node 直接 require 同一支檔案。**不會出現兩套規則互相漂移。**

### 4.4 斷線（照你的決定：一斷線就判輸）

- **不暫停、不給寬限秒數。** 只要連線真的斷掉就立刻判該玩家淘汰（判輸）。
  - 判定條件：WebSocket **close／error**，或**心跳連兩次沒回**（約 2 秒，這是「連線已死」的技術下限，
    不是給玩家的等待時間）。
- 對手畫面上顯示「對手斷線了」，並**照常繼續玩**，把自己的深度跑完才進結算。
- 斷線者重連回來以**觀戰者**身分進房，可以看完這局；下一局有空位就自己搶位。
- 不做 AI 接管。

---

## 5. AI 四段（差異要看得出來）

AI 只輸出 `-1 / 0 / 1`，跟真人走同一個輸入管道，不作弊（不偷看未生成的樓梯、不加速）。

| 難度 | 反應延遲 | 預看層數 | 會避刺／假階 | 會用彈簧 | 會推人 | 失誤率 |
|---|---|---|---|---|---|---|
| **幼幼班** | 500 ms | 1 層 | 幾乎不會 | 不會 | 不會 | 高（常常撞牆停住） |
| **簡單** | 320 ms | 2 層 | 會避刺，假階常踩 | 偶爾誤踩 | 不會 | 中 |
| **普通** | 180 ms | 3 層 | 都會避 | 會刻意避開 | 偶爾（順路才推） | 低 |
| **困難** | 80 ms | 5 層 | 都會避，還會挑寬階 | 會避，也會拿來救命 | **會刻意把你推去撞刺、推上輸送帶、卡在天花板下** | 很低 |

- 決策方式：對「往左／不動／往右」各評分 —— 落點安全性、離下一個安全落點的距離、
  離天花板的餘裕、對手位置（困難才加權）—— 取最高分，加上難度對應的延遲與失誤抖動。
- **幼幼班對戰不開放**，所以 AI 幼幼班只出現在「陪玩不比輸贏」的情境（例如你在旁邊示範給小朋友看）。
- AI 顯示名字＋難度，例如「小藍熊（困難）」。
- 驗收：`scripts/ai-check.js` 讓四段 AI 各跑 200 局，**平均深度必須單調遞增且差距穩定**。

---

## 6. 線上：大廳、房間、觀戰、邀請

- **只有公開房 ＋ 邀請連結**，不做密碼房（沿用 bubble-battle）。
- **大廳**：房間列表（房名、難度、2/2 或 1/2、觀戰人數）、**快速加入**（找不到空房就自動開一間等人）、
  Render 冷啟動時顯示「喚醒伺服器中」動畫並輪詢 `/health`。
- **房間**：2 個玩家席位 ＋ 觀戰席（上限 20 人）。
  - 房主可選難度（簡單／普通／困難），**兩人都按「準備好」**才能開始。
  - 房主可踢人；房主離開自動換人。
  - **房間沒人就立即關閉**（免費方案不留空房），關房時該房的邀請連結一起失效。
  - 「再來一局」**一定換新的樓梯 seed**，沒有「重打同一座」的選項。
- **觀戰**（依你的決定）
  - 第 3 人以後進房**自動成為觀戰者**（**上限 20 人**，滿了顯示「觀戰席已滿」並回大廳），
    可以聊天、**可以看到全場資訊**（兩人血量、深度、鏡頭）。
  - **不做候補排隊**：席位一空出來就**開放搶位**，觀戰者畫面上直接出現「坐下」按鈕，**先按先得**
    （對局進行中不開放搶位，等這局結算回房間才開）。
  - 觀戰者看到的**就跟玩家完全一樣**（同一個鏡頭、兩人深度與血量、難度與速度倍率、目前世界、聊天），
    資訊欄最上面多一行 **「你是觀戰者」** 標明身分。這款沒有隱藏資訊，不用做視野遮蔽；
    也**不另外給觀戰者玩家看不到的對照數據**（例如領先落後差距、即時統計），大家看到的是同一份資訊。
- **暱稱與角色**
  - 進大廳不強迫打字：預設給一個**隨機可愛名**，由「**形容詞＋小動物**」自動組合
    （藍藍小熊、飛快小兔、勇敢小雞、愛睏小貓……兩份詞庫互乘有幾百種，不容易撞名），
    **對戰時可以自己改**成想要的暱稱；記住上次用的暱稱與角色，下次自動帶入。
  - 同房角色可以重複：**自己腳下有光環、頭頂有暱稱，再加玩家色外框（玩家 1 藍、玩家 2 紅）**，
    兩個人選同一隻小朋友時，玩家自己和觀戰者都分得清楚。
- **聊天**：**自由輸入 ＋ 快捷短語**按鈕（加油！／好強！／小心！／哈哈／等我一下／再一局／謝謝／掰掰），
  不會打字的小朋友按按鈕就能溝通；觀戰者也能聊。
- **邀請連結**：`/?room=<id>&invite=<token>`，token 有**範圍（該房）、可撤銷、與房間同生命**。
  加入前由伺服器驗證，並對「無效／已撤銷／房間已滿（轉觀戰）／房間已結束」給看得懂的提示與複製／分享按鈕。
- 席位滿了不會偷偷把觀戰者當玩家；角色與權限在畫面上一直標明（你是玩家 1／玩家 2／觀戰者）。

---

## 7. 畫面與操作

### 7.1 版面（依你的偏好：資訊欄放左邊、刻意小、樓梯畫面最大）

- **桌機／平板橫向**：左邊一條**窄資訊欄**（約 260～300 px，不做右半邊 50/50），
  中間是**縱向長條的樓梯畫面**，右邊留白或放背景裝飾。
  - 資訊欄（上到下）：**自己深度／對手深度（只給數字，不顯示領先落後差距）**、雙方愛心血量、難度與目前速度倍率、
    目前世界（第幾層／下一層還有幾公尺）、連線與 AI 狀態、席位／觀戰人數、
    **左下角固定聊天室**（可收合、有未讀數、含快捷短語）。
- **平板直向／手機直向**：樓梯畫面撐滿主要區域（縱向最適合這款遊戲），資訊欄壓成**上方一條薄狀態列**
  （深度、雙方血量、難度），聊天室變成**左下角浮動按鈕＋彈出面板**，未讀數顯示在按鈕上。
- **手機橫向**：畫面高度不足 → 顯示「轉直向比較好玩」的提示（可關閉），
  但仍然完整可玩（縮短可見高度到 14 格並放大角色）。
- 檢查點：平板直／橫向 → 桌機寬版 → 手機直／橫向（你的優先序：平板＞桌機＞手機）。
- 資訊欄與聊天室**絕對不遮住左下觸控按鍵與樓梯畫面**；安全區（瀏海／圓角）都留邊。

### 7.2 操作

- **鍵盤**：← → 或 A D；`Esc` 暫停選單（單機）、`Enter` 送出聊天。
  **不做 R 快速重來**：重開一局一律從暫停選單或結算畫面按，避免小朋友誤觸把快破記錄的一局重置掉。
- **暫停選單（Esc）只有三顆**：繼續／重新開始／回首頁。設定不放進來（走右上角那顆，不做重複入口），
  幼幼班的「結束這局」也在右上角。暫停時鏡頭、計時與扣血全部停住。
- **觸控**：**左下角左右兩顆大按鍵**（依你的決定，不做全螢幕左右半邊、不做切換）。
  - 按鍵直徑至少 72 px，圖示是明確的箭頭，按下有立體按壓動畫與可關閉的輕震動。
  - 兩顆鍵之間留間距避免誤觸；支援手指從一顆滑到另一顆（不用放開再按）。
  - **橫向**：兩顆都在左下（左手拇指操作，右手扶著裝置）。
  - **直向**：改成**左鍵左下、右鍵右下**，兩手拇指各一邊，直向持機最自然。這是唯一的版面差異，不做設定切換。
- 沒有跳鍵、沒有下壓鍵，畫面上就只有兩顆鍵，小朋友一看就懂。
- **不支援遊戲手把**，只做鍵盤與觸控。
- SVG 立體按鈕風格（漸層＋內外陰影＋按壓位移）沿用既有小遊戲的 `svgui.js`。

### 7.3 開局與流程

- 首頁：**大主視覺（小朋友站在樓梯口往下看）＋三顆大按鈕**
  - **一個人玩** → 選難度（四段）→ 開始
  - **跟別人玩** → 「跟電腦對戰」（選 AI 難度）或「線上大廳」
  - **怎麼玩** → 靜態圖文說明頁
- **對戰開局**：兩人站在**同一個寬 8 格的大平台上、左右分開**（間距足夠，不會一開局就互推），
  **倒數 3 秒**（3－2－1－開始！）後才能動，倒數期間鏡頭不下捲。
- 一人挑戰同樣從出生大平台開始，倒數 2 秒。
- 每個畫面都有清楚的下一步與返回；載入／空白（大廳沒房）／錯誤／斷線／房間已結束都有專屬狀態畫面。

### 7.4 教學

**靜態圖文說明頁**（沿用 bubble-battle 的做法，不做互動練習關）：
SVG 圖解五種階梯各自的效果、天花板刺的危險（幼幼班版本畫成雲朵）、推擠示意、兩顆按鍵示意、
勝負規則（含「線上要兩個都死才結算」「跟電腦玩你死就結束」）。

### 7.5 設定（右上角按鈕 → Modal 彈窗）

主要畫面右上角一顆固定的設定鈕（窄版也在安全區內、不遮遊戲控制），按下開 Modal：

- **背景音樂**開關＋音量、**音效**開關＋音量（兩者獨立，預設都開）
- **震動**開關（觸控裝置）
- **減少動態**（關掉畫面震動與粒子）
- **色彩輔助**（強化形狀差異與外框；預設就靠形狀區分）
- **顯示深度輔助線**（每 10 公尺一條淡線）
- **清除本機紀錄**、**恢復預設**
- 幼幼班進行中，設定鈕旁邊另有一顆 **「結束這局」**（見 1.6）
- 設定**只有右上角這一個入口**，不重複放進 Esc 暫停選單
- 支援：首次使用者手勢後解鎖音訊、立即套用、重進仍保留、遮罩、焦點鎖定、
  Esc／返回鍵關閉、關閉後焦點回到原按鈕、螢幕閱讀器可操作。

### 7.6 結算

- 勝利者的**小朋友大頭貼＋一行數據**（深度、存活時間），下面一行是對手的數據。
- 再加一小區 **「這局統計」**：踩到刺幾次、被天花板頂了幾秒、踩到彈簧幾次、被推開幾次、到達的最深世界。
  單機一人挑戰也有這一區（「被推開」那項自動省略）。
- 線上：「再來一局」（回房間等房主開局，**換一座新樓梯**）／「回大廳」；
  單機：「再玩一次」（也是新樓梯）／「換難度」／「回首頁」。
- 破本機記錄時加一個小小的慶祝動畫（紙花＋音效）。

---

## 8. 視覺與音訊資產

- **全手繪向量 SVG，不使用 emoji 文字美術**（系統字型會影響長相）。
- **角色 8 隻小朋友**：頭＋身體＋雙手＋雙腳，漸層做立體感，不是圓球加臉；
  有走路、下墜、落地、踩刺、被頂、暈眩六組動畫姿勢。能力完全相同，只差外觀。
- **六套樓梯主題，隨深度每 100 公尺換下一層世界**（做成純資料檔 `public/js/themes/scenes.js`）：

  | 深度 | 世界 |
  |---|---|
  | 0～100 m | 幼稚園（教室、積木、彩色地墊） |
  | 100～200 m | 公園（草地、樹、鞦韆） |
  | 200～300 m | 海邊（沙、貝殼、浪花） |
  | 300～400 m | 森林（樹根、蕈菇、螢火蟲） |
  | 400～500 m | 糖果屋（棒棒糖、餅乾階、糖霜） |
  | 500 m 以上 | 太空站（金屬、星空、失重粒子） |

  - 六套用完後**循環並套用「夜間配色」變體**（同一套美術換色調），讓 600 m 以後還有新鮮感。
  - 換世界時做 **0.8 秒的過場漸變**＋慶祝提示：背景與階梯美術漸變過去、慶祝字樣顫一下就消失，
    **樓梯不停、操作不中斷、不擋畫面、不給休息平台**（開了「減少動態」就只換底色、不放粒子）。
  - 主題只換美術，**不改任何規則**（階梯機率、速度都由難度決定）。
- 階梯五款在每個主題裡都要**維持一眼可辨的形狀差異**（刺一定是尖三角、假階一定有裂痕、輸送帶一定有箭頭）。
- **音效**：腳步、落地、彈簧「啵」、假階碎裂、踩刺「唉呦」、被頂警告、扣血、100 公尺慶祝、淘汰、勝利。
  BGM 走**木琴童謠風**、循環；**同一段主旋律在每個世界換樂器變奏**
  （幼稚園木琴、公園口琴、海邊鋼片琴＋浪聲、森林木笛、糖果屋鐘琴、太空站電子音），
  換世界時順著過場切過去，不會聽起來像換了一款遊戲；速度倍率越高，節拍跟著加快。
  全部用可替換、授權清楚的暫用資產。

---

## 9. 檔案結構（預定）

```
stair-kids/
├─ server.js                  零依賴 HTTP + WebSocket、30Hz 房間迴圈、/health
├─ lib/
│  ├─ ws.js                   自寫 WebSocket（沿用 bubble-battle）
│  ├─ rooms.js                房間、2 席位、觀戰、搶位、邀請 token、權限
│  └─ match-loop.js           伺服器端 tick 與快照廣播
├─ public/
│  ├─ index.html
│  ├─ css/style.css
│  └─ js/
│     ├─ config.js            唯一 server URL 入口（GAME_SERVER_URL）
│     ├─ rules.js             ★ 共用純函式規則核心（物理、階梯、推擠、鏡頭、勝負）
│     ├─ stairs.js            樓梯程序生成器（可達性保證）
│     ├─ rng.js               可注入種子亂數
│     ├─ ai.js                四段 AI
│     ├─ net.js               預測／對帳／插值
│     ├─ online.js            大廳、房間、觀戰、搶位、聊天事件
│     ├─ render.js            繪圖（Canvas 畫場景、SVG 畫角色與 UI）
│     ├─ svgui.js             立體按鈕與共用 UI
│     ├─ input.js             鍵盤＋左下兩顆觸控鍵
│     ├─ audio.js  storage.js  app.js
│     └─ themes/
│        ├─ characters.js     8 隻小朋友
│        ├─ scenes.js         六套世界（隨深度切換）＋夜間變體
│        └─ nicknames.js      隨機暱稱詞庫（形容詞 × 小動物）
├─ tests/verify.js            規則核心單元測試
├─ scripts/
│  ├─ stairs-check.js         生成一萬層檢查可達性、比例、無死路
│  ├─ match-check.js          單機一局跑完（含電腦互打）
│  ├─ ai-check.js             四段 AI 深度差異
│  ├─ online-check.js         房間生命週期、2 席、觀戰、搶位、邀請
│  ├─ netcode-check.js        預測／對帳一致性（含 --lag=80）
│  ├─ result-check.js         勝負判定（兩人都死才結算、單機玩家死即結束、斷線判輸）
│  ├─ settings-check.js  sidebar-check.js
│  ├─ browser-check.js        Playwright 版面與觸控
│  └─ inject-server-url.js
├─ .env.example  .gitignore  render.yaml  README.md  啟動遊戲.bat
├─ 規劃書.md
└─ .github/workflows/          GitHub Pages 佈署（注入 GAME_SERVER_URL）
```

---

## 10. 里程碑（薄切優先，每階段都能玩）

| 階段 | 內容 | 驗收 |
|---|---|---|
| **M0** | 規則核心＋**單機一人挑戰一局可玩**：走位、重力、五種階梯、樓梯生成、鏡頭與天花板、血量、深度、100 m 換世界、結算、重新開始 | 規則單元測試全綠；`stairs-check` 一萬層零死路；一局能從開始玩到結算。**做完就整理成可 `git remote add` 的狀態，等你點頭建公開 repo** |
| **M1** | 四段難度定案＋**四段 AI**＋跟電腦對戰（同畫面共用樓梯、推擠、倒數開局、玩家死即結束） | `ai-check` 四段深度單調遞增；電腦互打能跑完 |
| **M2** | 線上：大廳、房間、2 人同步（30Hz 權威、預測／對帳／插值）、**兩人都死才結算**、**一斷線就判輸** | 兩台裝置同房順暢對戰；`netcode-check --lag=80` 通過 |
| **M3** | 觀戰、空位搶位、邀請連結、暱稱、聊天室＋快捷短語、左側資訊欄 | 角色權限與邀請流程正確（`online-check`） |
| **M4** | RWD（平板＞桌機＞手機、直橫向）、8 隻角色與六套世界美術、音效、教學頁、設定彈窗、本機紀錄、Render＋Pages 佈署準備、**加進既有 `game-lobby`** | `npm run verify` 全綠；大廳可直接進入 |

---

## 11. 測試與驗收清單

- **規則單元測試**：物理與落地、五種階梯各自效果（含輸送帶逆走 3 格／秒、彈簧一層半、刺 −1 ＋ 0.6 秒無敵）、
  推擠對稱性與貼牆、天花板扣血間隔、鏡頭保底速度與加速曲線、四段難度參數、100 m 換世界、
  **勝負四種情境**（線上兩人都死、線上斷線判輸、單機玩家死即結束、幼幼班手動結束）。
- **樓梯生成**：一萬層的可達性、特殊階比例、無連續死路、喘息階間隔、出生平台、同 seed 完全重現。
- **AI**：四段各 200 局的平均深度與勝率差異穩定可觀察。
- **線上**：房間生命週期、2 席滿轉觀戰、**空位搶位（對局中不開放）**、**沒人立即關房**、**觀戰滿 20 人的拒絕流程**、邀請 token 撤銷／失效、
  暱稱自訂與隨機、聊天與快捷短語、**觀戰者資訊欄與「你是觀戰者」標示**、斷線判輸後對手能玩完、重連回觀戰。
- **同步**：一般延遲與 `--lag=80` 下的對帳一致性，推擠不出現拉扯回彈。
- **版面**：平板直／橫、桌機寬、手機直／橫；**直向兩側各一顆鍵、橫向左下兩顆鍵**都要單手按得到；
  左側資訊欄與左下聊天室不遮觸控鍵；無水平溢出。
- **設定與音訊**：右上角彈窗流程、首次手勢解鎖音訊、獨立音量、重進保留、恢復預設、Esc 關閉、
  幼幼班「結束這局」。
- **暫停與過場**：Esc 選單三顆按鈕、暫停時鏡頭／計時／扣血都停住、0.8 秒換世界過場不中斷操作、
  幼幼班天花板畫成雲朵且不扣血。
- **其他**：server URL 參數化（不硬編碼、不回退 localhost）、Git-ready 檔案、正式建置。
- 所有項目彙整成 `npm run verify`；跑不動的項目會明確報告，不假裝通過。

---

## 12. 部署與版控

- **伺服器**：Render 免費方案（會休眠／冷啟動 → 房間狀態視為可清理的暫時狀態、不寫本機磁碟、提供 `/health`）。
  冷啟動時前端顯示「喚醒伺服器中」動畫（小朋友在樓梯口暖身）＋等待秒數，輪詢 `/health` 成功自動進大廳，逾時給錯誤與重試。
- **前端**：GitHub Pages ＋ GitHub Actions，建置時由 repo 變數 **`GAME_SERVER_URL`** 注入 `public/js/config.js`
  （必要時另設 `GAME_SERVER_WS_URL`）。正式走 HTTPS／WSS，明確設定允許的前端 origin／CORS。
- **本機**：雙擊 `啟動遊戲.bat` 或 `npm start` → <http://localhost:3060>。
- **版控（照你的決定）**：**現在就在 `C:\Eric\AI Agent\stair-kids` 建本機 git**，
  每個里程碑都繁中 commit，含 `.gitignore`、`.env.example`、lockfile；秘密與本機設定不進版控。
  **公開 GitHub repo 等 M0 可玩再建**（名稱 `stair-kids`）；建立與 push 需要你的帳號授權，
  我不會自行建立或 push，會先整理成可直接 `git remote add` 的狀態再請你點頭。
- **大廳整合**：完成後在 `game-lobby/config/games.json` 加一張卡（`id: stair-kids`、`localPort: 3060`、
  `presenceUrl: /api/presence`、badge 標 **「新遊戲」**、tags：單機／AI 對戰／線上 1v1／反應）。

---

## 13. 本機紀錄

用 `localStorage` 存**只在這台裝置上**的紀錄，不上傳、不做伺服器排行榜：

- 四段難度各自的**最深深度**與達成日期
- 到達過的最深世界（例如「去過糖果屋」）
- 對戰勝負場次（對電腦／線上分開計）
- 最常用的小朋友角色
- 上次用的暱稱與角色（下次自動帶入）

首頁與設定彈窗都能看，設定彈窗提供「清除紀錄」。

---

## 14. 我自己先決定、你可以否決的細節

規劃問答的決定都照你選的寫進上面了。以下是實作細節，我先定好，不同意就說：

1. **「一斷線就判輸」的技術判定**：真正的「斷線」＝ WebSocket close／error，或**心跳連兩次沒回（約 2 秒）**。
   這 2 秒是「確認連線已死」的技術下限，不是給玩家的寬限；沒辦法做到 0 秒。
2. **對局進行中不開放搶位**：席位要等這局結算回房間才開放搶（不然對局中間插一個人進來沒辦法接）。
3. **隨機暱稱詞庫**：形容詞（藍藍、飛快、勇敢、愛睏、圓圓、亮亮、胖胖、跳跳……）
   × 小動物（小熊、小兔、小雞、小貓、小狗、小企鵝、小青蛙、小熊貓……）；要換成你自己的清單直接給我。
4. **快捷短語 8 句**：加油！／好強！／小心！／哈哈／等我一下／再一局／謝謝／掰掰。
5. **六套世界用完後循環＋夜間配色**，不無限畫新主題。
6. **每 12 層一個寬 5 格的階**當喘息點（不是平台、不停鏡頭），純粹避免手感太窒息。
7. **手機橫向**只給提示不強制轉向，仍完整可玩。
8. **本機 port 3060**。
9. **正式美術與音訊**：我會先做出可用的手繪 SVG（8 隻小朋友、六套世界）與可替換的暫用音效；
   之後要換成指定資產，`public/js/themes/*.js` 純資料格式會保持可替換。

確認以上之後，就從 **M0（單機一人挑戰的可玩薄切）** 開始實作。
